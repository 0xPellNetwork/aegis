syntax = "proto3";
package xmsg;

option go_package = "github.com/0xPellNetwork/aegis/x/xmsg/types";

// in tx hash to xmsg
message InTxHashToXmsg {
  string in_tx_hash = 1;
  repeated string xmsg_indices = 2;
}
