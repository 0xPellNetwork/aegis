syntax = "proto3";
package pkg.proofs.ethereum;

option go_package = "github.com/pell-chain/pellcore/pkg/proofs/ethereum";

// Proof represents the proof of a ethereum transaction
message Proof {
  repeated bytes keys = 1;
  repeated bytes values = 2;
}
